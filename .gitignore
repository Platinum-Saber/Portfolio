node_modules/
.next/
out/
build/
.DS_Store
*.pem
.env*
!.env.example
.vercel
*.tsbuildinfo
next-env.d.ts
_to_delete/
logs/

supabase/.temp

# Unprocessed environment sources — a 467 MB staging folder, of which one file
# is 402 MB. GitHub's hard per-file limit is 100 MB, so this cannot be
# committed even with the raw-assets convention. Anything from here that ships
# gets copied into assets/raw/ first, and only if the source is small enough to
# live in git. See docs/PLAN.md Phase 3.
models/environments/

# Cloudflare / OpenNext build output and local secrets
.open-next/
.wrangler/
.dev.vars
